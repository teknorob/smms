<!DOCTYPE html>
<html>
<head>
  <title>Update Membership</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<h1>MembershipType#edit</h1>
<p>Find me in app/views/membership_type/create.html.erb</p>

<%= form_tag(member_update_membership_path, method: "get") do %>
<p>Editing Membership for <%= @member.tx_first_name + ' ' + @member.tx_last_name %> </p>
    <p>Current Membership Type Name:
    <% if @member.membership != nil? %>
        <%= @member.membership.membership_type.tx_membership_type %>
          - Expires <%= @member.membership.dt_expiration_date.strftime("%m/%d/%Y") %>
    <% else%>
        No current membership.
    <% end %>
    </p><br/>
    Membership Type:
    <% if @membership_types.length < 1 %>
        No membership types currently available.
    <% else %>
        <%= select_tag(:membership_type, options_from_collection_for_select(@membership_types, :id, :tx_membership_type )) %>
    <% end %>

  <input type="submit" value="Add this membership to member">
<% end %>
<%= button_to "Back to member", member_path(@member), :method => :get%>
</body>
</html>

